package com.tarun.foodsystem.model;

/**
 * Enum representing the available selection criteria for restaurant assignment.
 */
public enum SelectionCriteria {
    LOWEST_COST,
    HIGHEST_RATING,
    MAX_CAPACITY  // Bonus: Restaurant with maximum remaining capacity
}
